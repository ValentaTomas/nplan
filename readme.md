package it as binary
save journal folder into config
automatically sync with git repo
allow selection of archived journals with <<, and with direct date
allow syntax for cross-day tasks - automatically copy entries starting with "*", if they were not marked as completed (copy not just from previous day, but keep track of all active tasks)